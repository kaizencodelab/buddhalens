# BUDDHALENS
## Docker

Run Docker Build checks first:
```bash
docker build --check -t buddhalens .
```

Then, build the image:

```bash
docker build -t buddhalens .
```
Run the container (map host 8080 to container 80):

```bash
docker run -p 8080:80 buddhalens
```

